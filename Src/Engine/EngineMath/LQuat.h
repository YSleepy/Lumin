#pragma once
#include "LVec3.h"
namespace Lumin::Math
{
	struct LQuat
	{
		float x, y, z, w;

		//LQuat();
		//LQuat(float x_, float y_, float z_, float w_);

		//LQuat operator*(const LQuat& rhs) const;
		//LVec3 Rotate(const LVec3& v) const;

		//static LQuat FromAxisAngle(const LVec3& axis, float radians);
		//static LQuat Slerp(const LQuat& a, const LQuat& b, float t);
	};

}
